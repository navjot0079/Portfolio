# Frontend Environment Variables
# Create a .env file from this template for local development

# Backend API URL (for separate deployments)
# Local development: leave empty (uses /api/contact proxy)
# Production (Vercel): set to your Render backend URL
VITE_API_URL=https://your-backend.onrender.com/api/contact
